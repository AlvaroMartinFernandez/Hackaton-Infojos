
  {
    "version": 2,
    "functions": {
      "api": {
        "handler": "index.handler",
        "runtime": "@vercel/node",
        "maxDuration": 60
      }
    },
    "routes": [
      {
        "src": "/(.*)",
        "dest": "api"
      }
    ]
  }
  